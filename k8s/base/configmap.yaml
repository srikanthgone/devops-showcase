apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-showcase-config
  labels:
    app.kubernetes.io/name: devops-showcase
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  PORT: "8080"
  READ_TIMEOUT: "5s"
  WRITE_TIMEOUT: "10s"
  IDLE_TIMEOUT: "120s"
  SHUTDOWN_TIMEOUT: "15s"
